[package]
name = "hello_macro_derive"
version = "0.1.0"
edition = "2024"

[lib]
proc-macro = true

[dependencies]
# syn="2.0"
# Update this line in your [dependencies]
syn = { version = "2.0", features = ["full"] }
quote="1.0"

# [workspace]
# members = []
