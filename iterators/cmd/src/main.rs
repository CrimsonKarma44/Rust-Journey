// mod test;
use utils::{dot_iter_types::{iter_mut, into_iter, iter}, just_tests::trial};
fn main() {

    // using .iter_mut(), into_iter(), and iter()
    iter_mut();
    into_iter();
    iter();

    // running just_tests::trial()
    trial();
    
}
